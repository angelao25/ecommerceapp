<div id="favorite">
  <% if product.favorite %>
    <%= link_to 'Dislike Button', favorite_path(product.id), data: {
    turbo_method: :delete
    } %>
  <% else %>
    <%= link_to 'Like Button', favorites_path(product_id: product.id), data: {
    turbo_method: :post
} %>
  <% end %>
</div>
